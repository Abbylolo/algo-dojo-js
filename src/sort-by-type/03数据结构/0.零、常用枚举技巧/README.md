# 零、常用枚举技巧
## 一、枚举右，维护左
对于双变量问题，例如两数之和 a[i] + a[j] = t，可以枚举右边的 a[j]，将其转化为单变量问题：在 a[j] 左侧查找是否存在 a[i] = t − a[j]，这一过程可用哈希表高效维护。  
该技巧称为“枚举右，维护左”。
> 很多涉及到「两个变量」的题目，都可以枚举其中一个变量，把它当成常量看待，从而转化成「一个变量」的问题。代码实现时，通常来说「枚举右，寻找左」是更加好写的。

## 二、枚举中间
### 1. 题目类型
对于有三个或者四个变量的问题，枚举中间的变量往往更好算。
为什么？比如问题有三个下标，需要满足 0≤i<j<k<n，对比一下：
- 枚举 i，后续计算中还需保证 j<k。
- 枚举 j，那么 i 和 k 自动被 j 隔开，互相独立，后续计算中无需关心 i 和 k 的位置关系。
所以枚举中间的变量更简单。

### 2. 解题思路
前后缀分解

## 三、枚举对角线
### 1. 题目类型
对于矩阵问题，枚举对角线是一种常用技巧。
> 例如，矩阵中的对角线遍历问题，就是枚举对角线。

### 2. 解题思路
准备: 根据点移动规律找到对角线公式，如：该方向对角线每移动一个点：i减少1个，j增加一个，所以每移动一个点i+j=0 => 对角线上点i+j是常数
1. 遍历对角线: k=i+j+1 => 范围[1, m+n-1]
2. 遍历对角线上点：遍历j，j=k-i-1 => jmin = max(k-m,0),jmax = min(k-1,n-1)
3. 奇偶遍历点
